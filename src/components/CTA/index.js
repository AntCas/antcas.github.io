import React from 'react'

import Email from '../Email';

import './index.scss'

const CTA = () => (
  <div className="cta">
    <h2>Want to work with me on a project like this? </h2>
    <Email />
  </div>
);

export default CTA;
